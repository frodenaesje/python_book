# file: ex_06_10_args_stats_start.py

# TODO: Write total(*numbers: float) -> float
#       Returns the sum of all arguments.
#       Do NOT use the built-in sum() - calculate manually with a loop.

# TODO: Write average(*numbers: float) -> float
#       Returns the mean of all arguments.
#       Hint: reuse total() or calculate directly.

# TODO: Write stats(*numbers: float) -> tuple
#       Returns (min, max, mean) as a tuple.
#       Do NOT use built-in min() or max() - find them with a loop.

if __name__ == "__main__":
    # TODO: Call and print results for all three functions
    #       Use different argument counts to show *args flexibility
    #       Example:
    #         print(f"total(1, 2, 3):          {total(1, 2, 3)}")
    #         print(f"average(1, 2, 3, 4, 5):  {average(1, 2, 3, 4, 5)}")
    #         low, high, mean = stats(4, 7, 2, 9, 1)
    #         print(f"stats(4, 7, 2, 9, 1):    min={low}, max={high}, mean={mean}")
    pass
