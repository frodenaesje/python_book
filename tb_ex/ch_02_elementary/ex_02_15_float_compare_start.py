# file: ex_02_15_float_compare_start.py
import math

a = float(input("Enter the first number: "))
b = float(input("Enter the second number: "))
expected = float(input("Enter the expected sum: "))

# TODO: compute a + b

# TODO: print the sum, then compare it to expected in two
#   ways and print both results:
#     - with ==            (unreliable for floats)
#     - with math.isclose  (the safe way)
#   Try 0.1, 0.2 and 0.3 to see the difference.
